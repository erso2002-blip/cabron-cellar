# v0.2.3

- Public app version: `0.2.3`
- Status: release candidate pending production deploy.

## Changes

- Added an `Instalar app` CTA inside the desktop and mobile app menu.
- Captures the native browser PWA install prompt when available.
- Hides the CTA when the app is already running in installed/standalone mode.
- Shows short iPhone Safari guidance: `Compartilhar > Adicionar à Tela de Início`.

## Validation

- `pnpm --filter @workspace/minha-adega typecheck`
- `PORT=3000 BASE_PATH=/ pnpm --filter @workspace/minha-adega build`

## Notes

- No database migration required.
- The CTA is non-blocking and does not use an aggressive pop-up.
