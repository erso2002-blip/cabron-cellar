# v0.2.9

- Public app version: `0.2.9`
- Fixes `Sua Adega` search by filtering the loaded wine list in the client.
- Search now normalizes accents and case before matching.
- Search fields: name, producer, winery website, grape, country, and region.
- Service worker cache bumped to `minha-adega-v7` to force fresh delivery.

## Validation

- `pnpm --filter @workspace/minha-adega typecheck` passed.
- `pnpm --filter @workspace/api-server typecheck` passed.
- `pnpm --filter @workspace/api-server build` passed.
- `PORT=3000 BASE_PATH=/ pnpm --filter @workspace/minha-adega build` passed.
- `git diff --check` passed.
- Commit `ec70b8a` created locally.
- Production deployment `cabron-cellar-14mcc4vw4-erso2002-blips-projects.vercel.app` became `Ready`.
- `https://mycellar.com.br/` returned 200.
- `https://www.mycellar.com.br/` returned 200.
- `https://mycellar.com.br/api/healthz` returned `{"status":"ok"}`.
- Public `sw.js` contains `minha-adega-v7`.
- Public bundle contains `0.2.9` and the updated client-side search logic.
