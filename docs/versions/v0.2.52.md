# v0.2.52

Release date: 2026-06-22

## Changes

- Fix dashboard stock metrics so consumed wines with `quantity = 0` no longer count as active unique labels.
- Update 12-month attention count to sum active bottles instead of wine rows.
- Filter top country/region stats to active stock only.
- Bump app/API/workspace version to `0.2.52`, native build numbers to `52`, and service worker cache to `minha-adega-v47`.

## Validation

- Front-end TypeScript passed.
- API TypeScript passed.
- API production build passed.
- Front-end production build passed with known non-blocking sourcemap/chunk warnings.
- Capacitor sync for iOS/Android passed.

## Rollback

- Roll back app deploy to `v0.2.51` if dashboard stock metrics regress.
