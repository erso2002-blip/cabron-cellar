# v0.2.13

- Public app version: `0.2.13`
- Renames the dashboard urgency card from "Beber em Breve" to "Atenção 12 Meses".
- Renames the dashboard section to "Atenção nos Próximos 12 Meses".
- Updates the user-facing criterion to bottles that should be evaluated or consumed within the next 12 months.
- Changes dashboard stats from a 180-day cutoff to a 12-month cutoff.
- Filters `/api/wines/drink-soon` to return only bottles overdue or within the next 12 months.
- Service worker cache bumped to `minha-adega-v11` to force fresh delivery.

## Validation

- `pnpm --filter @workspace/api-server typecheck` passed.
- `pnpm --filter @workspace/minha-adega typecheck` passed.
- `pnpm --filter @workspace/api-server build` passed.
- `PORT=3000 BASE_PATH=/ pnpm --filter @workspace/minha-adega build` passed.
- Production deploy `cabron-cellar-md6em0bba-erso2002-blips-projects.vercel.app` reached `READY`.
- Alias `https://mycellar.com.br` was applied.
- Public `/` returned HTTP 200.
- Public bundle contains `0.2.13`, `Atenção 12 Meses`, `Atenção nos Próximos 12 Meses`, and the 12-month description.
- `/api/healthz` returned `{"status":"ok"}`.
- Public service worker contains `minha-adega-v11`.
