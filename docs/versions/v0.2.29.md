# v0.2.29

## Changes

- Show label-reading AI as an included Basic/Free plan feature.
- Keep Pro positioning focused on scale, harmonization, and recommendations.
- Bump workspace version to `0.2.29`.

## Validation

- `pnpm --filter @workspace/api-server typecheck`
- `pnpm run typecheck`
- `pnpm --filter @workspace/api-server build`
- `PORT=3000 BASE_PATH=/ pnpm --filter @workspace/minha-adega build`
- GitHub commit/tag `9ee7a1e` / `v0.2.29` pushed to `origin/main`.
- Production `/api/healthz` returned `{"status":"ok"}`.
- Production `/billing` returned HTTP 200.
- Production `/api/billing/plans` returned Free features including `Leitura de rotulo por IA`.
- Production `/api/billing/plans` returned Pro features using `IA, harmonizacao e recomendacao`.

## Rollback

- Roll back app deploy to `v0.2.28` if the billing plan copy causes unexpected commercial positioning issues.
