# v0.1.0

## Status

- Production: yes
- Public app version: `0.1.0`
- Stable rollback target: yes

## Release

- Date: 2026-06-16
- Git commit: `b22f29f`
- Production URL: `https://cabron-cellar.vercel.app`
- Product name: `MyCellar`

## Summary

- Removed the old Replit/Clerk access block.
- Deployed the app on Vercel with API routing and Neon/Postgres connected.
- Added visible app version in the interface.
- Fixed photo persistence fallback and wine consumption flow.
- Configured OpenAI key for label analysis.
- Added hardening and Google SSO rollout without breaking the app before OAuth setup.
- Activated Google SSO in production after Vercel environment variables were configured.

## Validation

- Frontend returned HTTP 200 in production.
- `/api/healthz` returned HTTP 200.
- Google SSO config returned `configured:true` after environment setup.
- Unauthenticated protected API routes returned HTTP 401.
- Label analysis endpoint passed production test after OpenAI key replacement.

## Known Gaps

- Validate real Google login with `mycellarwines@gmail.com`.
- Confirm legacy cellar migration after first login.
- Configure `mycellar.com.br` DNS and OAuth authorized origins.
- Replace or remove exposed temporary password before external testing.
- Complete visual/manual PWA validation.

## Rollback

- Preferred rollback: Vercel rollback to the production deployment associated with commit `b22f29f`.
- Code rollback: redeploy Git tag `v0.1.0` or commit `b22f29f`.
- Database rollback: no destructive schema rollback required for this version.

