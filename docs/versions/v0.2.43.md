# v0.2.43

## Changes

- Prepare the first internationalization layer for Brazil, Argentina, and Chile.
- Keep Brazil as the default market so the current client experience remains unchanged.
- Add shared market configuration for locale and currency formatting.
- Expose the active backend market in `/api/billing/plans`.
- Centralize front-end currency and date formatting instead of hardcoded `pt-BR` and `BRL` calls.
- Add backup and restore documentation plus the daily backup script scaffold.
- Bump app/API/workspace version to `0.2.43` and service worker cache to `minha-adega-v38`.

## Validation

- `pnpm --filter @workspace/minha-adega typecheck` passed.
- `pnpm --filter @workspace/api-server typecheck` passed.
- `PORT=3000 BASE_PATH=/ pnpm --filter @workspace/minha-adega build` passed with known non-blocking sourcemap/chunk warnings.
- `pnpm --filter @workspace/api-server build` passed.
- `VITE_MYCELLAR_MARKET=AR PORT=3000 BASE_PATH=/ pnpm --filter @workspace/minha-adega build` passed and generated an Argentina/ARS/es-AR bundle.
- `mycellar-production-backup.timer` was enabled for daily local backups.
- Manual service execution created backup `2026-06-21T015818Z`.
- Production deploy `dpl_CS14C21Vwh5bq3hPmTBNEwz2km5U` reached `READY`.
- Alias `https://mycellar.com.br` was applied.
- `https://mycellar.com.br` returned HTTP 200.
- `/api/healthz` returned `{"status":"ok"}`.
- `/api/billing/plans` returned default market `BR` / `pt-BR` / `BRL`.
- Public `sw.js` contains `minha-adega-v38`.
- Public bundle contains `0.2.43` and corrected subscription-plan copy.

## Rollback

- Roll back app deploy to `v0.2.42` if the market configuration layer causes unexpected UI, billing, or formatting issues.
