# v0.2.42

## Changes

- Standardize the subscription screen copy according to written Portuguese norms.
- Replace English or unaccented labels such as `Free`, `Gratis`, `Promocao de lancamento`, `Ate`, `rotulo`, `Historico basico`, and `Economia vs. mensal`.
- Bump app/API/workspace version to `0.2.42` and service worker cache to `minha-adega-v37`.

## Validation

- `pnpm run typecheck` passed.
- `pnpm -r --filter "./artifacts/minha-adega" --if-present run build` passed with known non-blocking sourcemap/chunk warnings.
- Production deploy `dpl_98fTELe4VpgzLBe71jHhRaWcLiJu` reached `READY`.
- `https://mycellar.com.br` returned HTTP 200.
- `/api/healthz` returned `{"status":"ok"}`.
- Public `sw.js` contains `minha-adega-v37`.
- Public billing API and front-end bundle contain the corrected Portuguese copy.

## Rollback

- Roll back app deploy to `v0.2.41` if the subscription copy change causes unexpected UI issues.
