# v0.2.6

- Public app version: `0.2.6`
- Shows the PWA install call-to-action directly in the mobile app body, not only inside the menu.
- On iPhone, displays the Safari instruction immediately because iOS does not expose the automatic install prompt.
- Adds a safe winery-site fallback for existing Achaval Ferrer records that were created before `wineryWebsiteUrl` existed.
- Service worker cache bumped to `minha-adega-v4` to force fresh delivery.

## Validation

- Pending before production: front typecheck, front build, public deploy checks.
