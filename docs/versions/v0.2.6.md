# v0.2.6

- Public app version: `0.2.6`
- Shows the PWA install call-to-action directly in the mobile app body, not only inside the menu.
- On iPhone, displays the Safari instruction immediately because iOS does not expose the automatic install prompt.
- Adds a safe winery-site fallback for existing Achaval Ferrer records that were created before `wineryWebsiteUrl` existed.
- Service worker cache bumped to `minha-adega-v4` to force fresh delivery.

## Validation

- `pnpm --filter @workspace/minha-adega typecheck` passed.
- `PORT=3000 BASE_PATH=/ pnpm --filter @workspace/minha-adega build` passed.
- `git diff --check` passed.
- Production deployment became `Ready`.
- `https://mycellar.com.br/` returned 200.
- `https://www.mycellar.com.br/` returned 200.
- `https://mycellar.com.br/api/healthz` returned `{"status":"ok"}`.
- Public `sw.js` contains `minha-adega-v4`.
- Public bundle contains `0.2.6`, `Instalar app`, iPhone add-to-home-screen guidance, and Achaval Ferrer fallback.
