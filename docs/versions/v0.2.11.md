# v0.2.11

- Public app version: `0.2.11`
- Changes the consumption history list to show a compact bottle summary by default.
- Opens the full consumed-bottle details in a dedicated detail dialog when the history item is selected.
- Keeps full wine and consumption information available: label photo, producer, winery website, country, region, grape, vintage, price, location, cellar, shelf, drink-until, stock, notes, occasion, quantity consumed, personal note, and buy-again flag.
- Service worker cache bumped to `minha-adega-v9` to force fresh delivery.

## Validation

- `pnpm --filter @workspace/api-server typecheck` passed.
- `pnpm --filter @workspace/minha-adega typecheck` passed.
- `pnpm --filter @workspace/api-server build` passed.
- `PORT=3000 BASE_PATH=/ pnpm --filter @workspace/minha-adega build` passed.
