# v0.2.58

## Status

Production candidate.

## Changes

- Keeps official winery, producer PDF, and stronger sourced drink-date suggestions as the preferred basis.
- Replaces low-confidence, no-source, and profile-only drink-date results with a deterministic profile estimate based on grape, style, region, country, and vintage.
- Bumps the PWA service worker cache to force clients onto the corrected bundle.

## Validation

- `pnpm --filter @workspace/api-server typecheck`
- `pnpm --filter @workspace/minha-adega typecheck`
- `pnpm --filter @workspace/api-server build`
- `pnpm --filter @workspace/minha-adega build`

## Rollback

- Revert to Git tag `v0.2.57` or roll back the previous Vercel production deployment.
