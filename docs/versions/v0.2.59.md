# v0.2.59

## Changes

- Persists drink-date suggestion metadata on wine records: source URL, source title, source type, confidence, and reason.
- Shows the source/confidence line directly under "Beber até" on the main wine detail screen.
- Keeps the AI suggestion explanation visible when reopening the edit form for wines that already have stored metadata.

## Validation

- `pnpm run typecheck`
- `pnpm --filter @workspace/minha-adega build`
- `pnpm --filter @workspace/api-server build`
