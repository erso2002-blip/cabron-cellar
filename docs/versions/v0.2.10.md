# v0.2.10

- Public app version: `0.2.10`
- Fixes mobile label-scan actions so `Nova foto` stays inside the scan card.
- Expands consumption history records with full wine details: winery website, country, region, grape, vintage, price, location, cellar, shelf, drink-until, current stock, label photo, and bottle notes.
- Updates the history screen to show both wine details and consumption details for consumed bottles.
- Service worker cache bumped to `minha-adega-v8` to force fresh delivery.

## Validation

- `pnpm --filter @workspace/api-server typecheck` passed.
- `pnpm --filter @workspace/minha-adega typecheck` passed.
- `pnpm --filter @workspace/api-server build` passed.
- `PORT=3000 BASE_PATH=/ pnpm --filter @workspace/minha-adega build` passed.
