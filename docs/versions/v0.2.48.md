# v0.2.48

Release date: 2026-06-22

## Changes

- Add authenticated account profile endpoints to read and update name/phone.
- Add authenticated notification preference endpoints to read and persist app communication preferences.
- Add authenticated account deletion request endpoint with auditable pending request storage.
- Replace profile mailto actions with in-app API flows for editing profile, notification settings, and deletion request.
- Add idempotent production schema bootstrap for account profile phone, notification preferences, and account deletion requests.
- Bump app/API/workspace version to `0.2.48`, native build numbers to `48`, and service worker cache to `minha-adega-v43`.

## Validation

- Front-end TypeScript passed.
- API TypeScript passed.
- API production build passed.
- Front-end production build passed.
- Capacitor sync for iOS/Android passed.
- Vercel production deployment `dpl_wubBapx4Tva5baoqHqWh4Xeqy3T4` reached `READY` and alias `https://mycellar.com.br` was applied.
- Public validation passed for `/api/healthz`, `/profile`, service worker cache `minha-adega-v43`, and unauthenticated `401` protection on the new account endpoints.
- Public bundle contains `0.2.48`, `Editar perfil`, `Notificações`, `Solicitar exclusão`, `/api/account/profile`, `/api/account/notifications`, and `/api/account/deletion-request`.
- Full monorepo build still requires `PORT` for the unrelated mockup sandbox package.

## Rollback

- Roll back app deploy to `v0.2.47` if the new profile/account endpoints cause unexpected account-screen issues.
