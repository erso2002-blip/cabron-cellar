# v0.2.15

## Summary

- Normalizes `Site da vinícola` values in the wine form so users can type domains with or without `https://`.
- Saves normalized website URLs from manual entry and label-scan results.
- Opens winery links safely from wine detail and consumption history even if older records were missing the protocol.
- Shows the winery website domain in `Sua Adega` cards/list rows when the wine has a website saved.
- Bumps the PWA service worker cache to `minha-adega-v13` to force fresh delivery.

## Validation

- `pnpm --filter @workspace/minha-adega typecheck` passed.
- `PORT=3000 BASE_PATH=/ pnpm --filter @workspace/minha-adega build` passed.

## Production

- Commit/tag GitHub: `74c4c59` / `v0.2.15`.
- Production deploy: `cabron-cellar-3do4jpzj1-erso2002-blips-projects.vercel.app`.
- Alias applied: `https://mycellar.com.br` and `https://www.mycellar.com.br`.
- Public validation: `/api/healthz` returned `{"status":"ok"}`.
- Public validation with cache-bust: `/sw.js?check=v0.2.15` contains `minha-adega-v13`.
- Public bundle contains `Site da vinícola` and the updated `vinicola.com.br` field guidance.
