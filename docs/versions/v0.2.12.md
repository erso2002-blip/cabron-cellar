# v0.2.12

- Public app version: `0.2.12`
- Updates the public `/waitlist` form for Instagram/Bio traffic.
- Adds required qualification fields: bottle count, use type, current control method, and beta interest.
- Replaces the generic notes prompt with the lead's main cellar difficulty.
- Stores the qualification answers in the existing `notes` payload, avoiding a production database migration.
- Service worker cache bumped to `minha-adega-v10` to force fresh delivery.

## Validation

- `pnpm --filter @workspace/minha-adega typecheck` passed.
- `PORT=3000 BASE_PATH=/ pnpm --filter @workspace/minha-adega build` passed.
- Production deploy `cabron-cellar-4joodd721-erso2002-blips-projects.vercel.app` reached `READY`.
- Alias `https://mycellar.com.br` was applied.
- Public `/waitlist` returned HTTP 200.
- Public bundle contains `0.2.12`, the new qualification fields, and `waitlist-public`.
- `/api/healthz` returned `{"status":"ok"}`.
- Public service worker contains `minha-adega-v10`.
