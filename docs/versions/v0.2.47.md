# v0.2.47

Release date: 2026-06-22

## Changes

- Clarify the Excluir conta action as an email-based request with validation and safe data handling.
- Include account name and email in the generated deletion request email body.
- Add an internal account deletion runbook for validated manual handling while automated in-app deletion is not active.
- Bump app/API/workspace version to `0.2.47`, native build numbers to `47`, and service worker cache to `minha-adega-v42`.

## Validation

- Front-end TypeScript passed.
- API TypeScript passed.
- Front-end production build passed.

## Rollback

- Roll back app deploy to `v0.2.46` if the account request copy or PWA cache update causes unexpected profile-screen issues.
