# v0.2.37

## Changes

- Restore stock quantity visibility in the compact cellar list card on mobile.
- Keep the side quantity column narrow on small screens to avoid reintroducing horizontal overflow.
- Bump app/API/workspace version to `0.2.37` and service worker cache to `minha-adega-v33`.

## Validation

- `pnpm --filter @workspace/minha-adega typecheck` passed.
- `PORT=3000 BASE_PATH=/ pnpm --filter @workspace/minha-adega build` passed with known non-blocking sourcemap/chunk warnings.

## Rollback

- Roll back app deploy to `v0.2.36` if the compact card change causes unexpected mobile layout issues.
