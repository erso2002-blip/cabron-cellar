# v0.2.16

## Summary

- Adds `Termos de Uso` and `Privacidade` links to the desktop and mobile side menu.
- Keeps legal pages available at `/termos` and `/privacidade`.
- Keeps login access gated by legal acceptance before Google or e-mail code login.
- Aligns app/package version display to `0.2.16`.
- Bumps the PWA service worker cache to `minha-adega-v14`.

## Validation

- `pnpm --filter @workspace/minha-adega typecheck` passed.
- `PORT=3000 BASE_PATH=/ pnpm --filter @workspace/minha-adega build` passed.

## Production

- Commit/tag GitHub: `d24285f` / `v0.2.16`.
- Production deploy: `cabron-cellar-o7xwse02d-erso2002-blips-projects.vercel.app`.
- Alias applied: `https://mycellar.com.br`.
- Public validation: `/api/healthz` returned `{"status":"ok"}` on `mycellar.com.br` and `www.mycellar.com.br`.
- Public validation: `/termos` and `/privacidade` returned HTTP 200.
- Public validation with cache-bust: `/sw.js?check=v0.2.16` contains `minha-adega-v14`.
- Public bundle contains `0.2.16`, legal routes, `Termos de Uso`, and `Privacidade`.
