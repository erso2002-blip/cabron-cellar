# v0.2.28

## Changes

- Add reverse pairing flow at `/pairing`.
- Add `Harmonizar` to the app menu.
- Add authenticated `POST /api/pairings/dish` to return 3 recommended wines from the user's available cellar stock.
- Keep AI recommendations constrained to wine IDs from the user's own cellar.
- Bump app version to `0.2.28` and service worker cache to `minha-adega-v25`.

## Validation

- `pnpm --filter @workspace/minha-adega typecheck`
- `pnpm --filter @workspace/api-server typecheck`
- `pnpm --filter @workspace/minha-adega build`
- `pnpm --filter @workspace/api-server build`
- Production deploy `dpl_3sgCYNksazoCqQpGXvAcExPoL4su` reached `READY` and alias `https://mycellar.com.br` was applied.
- Production `/api/healthz` returned `{"status":"ok"}`.
- Production `/pairing` returned HTTP 200.
- Production `/api/pairings/dish` without login returned controlled HTTP 401.
- Production `/sw.js` contains `minha-adega-v25`.
- Production bundle contains `0.2.28`, `Harmonizar`, `Sugerir 3 vinhos`, and `/api/pairings/dish`.
- Production `/api/billing/plans` still returns Pro Mensal `amount=25.45`, `originalAmount=49.9`; Pro Anual `amount=254.5`, `originalAmount=499`.

## Rollback

- Roll back app deploy to `v0.2.27` if the reverse pairing screen or API causes unexpected behavior.
