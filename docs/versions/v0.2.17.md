# v0.2.17

## Summary

- Applies Portuguese orthographic corrections to `Termos de Uso` and `Política de Privacidade`.
- Changes the legal links on the login screen from full page reloads to internal app navigation.
- Changes the legal page back action to internal app navigation, avoiding a blank frozen screen when returning to login.
- Aligns app/package version display to `0.2.17`.
- Bumps the PWA service worker cache to `minha-adega-v15` to force clients to refresh the legal-page fix.

## Validation

- `pnpm --filter @workspace/minha-adega typecheck` passed.
- `PORT=3000 BASE_PATH=/ pnpm --filter @workspace/minha-adega build` passed.
- Local production bundle contains `0.2.17`, `Política de Privacidade`, `Última atualização`, and `Voltar ao MyCellar`.
- Local service worker contains `minha-adega-v15`.

## Production

- Commit/tag GitHub: `6ec0b2e` / `v0.2.17`.
- Alias applied by Vercel automatic production deploy: `https://mycellar.com.br`.
- Public validation: `/api/healthz` returned `{"status":"ok"}`.
- Public validation: `/termos` and `/privacidade` returned HTTP 200.
- Public validation with cache-bust: `/sw.js?check=final-v0.2.17` contains `minha-adega-v15`.
- Public bundle contains `0.2.17`, `Política de Privacidade`, and `Última atualização`.
- Lacuna: no authenticated browser/PWA flow was changed or tested in this release; the fix targets public legal routes and login navigation.
