# v0.2.23

## Summary

- Make `/billing` public so plans can be viewed without login.
- Keep Mercado Pago checkout protected behind approved beta login.
- Change paid plan buttons to send unauthenticated users to `/login`.
- Improve closed beta e-mail login error copy.
- Bump app version to `0.2.23` and service worker cache to `minha-adega-v21`.

## Validation

- `pnpm --filter @workspace/minha-adega typecheck`
- `pnpm --filter @workspace/api-server typecheck`
- `PORT=3000 BASE_PATH=/ pnpm --filter @workspace/minha-adega build`
- `pnpm --filter @workspace/api-server build`

## Rollback

- Roll back app deploy to `v0.2.22` if the public billing page causes unexpected routing behavior.
