# v0.2.33

## Changes

- Rename the wine form field from `Foto do rotulo` to `Fotos adicionais`.
- Clarify that the field can be used for glass, back-label, or other wine-detail photos.
- Keep storage controlled by limiting the form to one additional photo per wine; a new upload replaces the previous image.
- Bump front-end/API/workspace version and service worker cache.

## Validation

- `pnpm --filter @workspace/minha-adega typecheck` passed.
- `PORT=3000 BASE_PATH=/ pnpm --filter @workspace/minha-adega build` passed with known non-blocking sourcemap/chunk warnings.

## Rollback

- Roll back app deploy to `v0.2.32` if the form copy causes user confusion.
