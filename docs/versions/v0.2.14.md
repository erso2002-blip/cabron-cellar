# v0.2.14

- Public app version: `0.2.14`
- Replaces the public waitlist header CTA from active "Abrir app" link to a non-clickable "App em beta fechado" state.
- Keeps Instagram/Bio traffic focused on the waitlist form while the app remains in test.
- Service worker cache bumped to `minha-adega-v12` to force fresh delivery.

## Validation

- `pnpm run typecheck` passed.
- `PORT=3000 BASE_PATH=/ pnpm --filter @workspace/minha-adega build` passed.
- Production deploy `cabron-cellar-hw5wy618m-erso2002-blips-projects.vercel.app` reached `READY`.
- Alias `https://mycellar.com.br` was applied.
- Public `/waitlist` returned HTTP 200.
- Public bundle contains `0.2.14` and `App em beta fechado`.
- `/api/healthz` returned `{"status":"ok"}`.
- Public service worker contains `minha-adega-v12`.
