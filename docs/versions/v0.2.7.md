# v0.2.7

- Public app version: `0.2.7`
- Removes the hardcoded Achaval Ferrer winery-site fallback from the wine detail screen.
- The winery website link now appears only when the selected wine record has `wineryWebsiteUrl`.
- Keeps the PWA install guidance only inside the desktop/mobile side menu.
- Service worker cache bumped to `minha-adega-v5` to force fresh delivery.

## Validation

- `pnpm --filter @workspace/minha-adega typecheck` passed.
- `PORT=3000 BASE_PATH=/ pnpm --filter @workspace/minha-adega build` passed.
- `git diff --check` passed.
- Commit `d801f7c` and tag `v0.2.7` were pushed to GitHub.
- Production deployment `cabron-cellar-649fxjcfv-erso2002-blips-projects.vercel.app` became `Ready`.
- `https://mycellar.com.br/` returned 200.
- `https://www.mycellar.com.br/` returned 200.
- `https://mycellar.com.br/api/healthz` returned `{"status":"ok"}`.
- Public `sw.js` contains `minha-adega-v5`.
- Public bundle contains `0.2.7`, does not contain `achaval-ferrer`, and keeps `Instalar app` only in the side-menu component.
