# v0.2.7

- Public app version: `0.2.7`
- Removes the hardcoded Achaval Ferrer winery-site fallback from the wine detail screen.
- The winery website link now appears only when the selected wine record has `wineryWebsiteUrl`.
- Keeps the PWA install guidance only inside the desktop/mobile side menu.
- Service worker cache bumped to `minha-adega-v5` to force fresh delivery.

## Validation

- `pnpm --filter @workspace/minha-adega typecheck` passed.
