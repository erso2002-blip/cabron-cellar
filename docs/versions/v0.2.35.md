# v0.2.35

## Changes

- Fix mobile horizontal overflow that could make the `Adega` screen open cut off on the right side.
- Add max-width/min-width guards to the shell, main content, cellar filters, and wine cards.
- Hide the side quantity block on very narrow list cards to preserve the wine information column.
- Bump app/API/workspace version to `0.2.35` and service worker cache to `minha-adega-v31`.

## Validation

- `pnpm --filter @workspace/minha-adega typecheck` passed.
- `pnpm --filter @workspace/api-server typecheck` passed.
- `PORT=3000 BASE_PATH=/ pnpm --filter @workspace/minha-adega build` passed with known non-blocking sourcemap/chunk warnings.
- Local Chrome mobile viewport `390x844` on `/wines` measured `scrollWidth=390`, `clientWidth=390`, `overflow=false`.

## Rollback

- Roll back app deploy to `v0.2.34` if the mobile layout fix causes unexpected display issues.
