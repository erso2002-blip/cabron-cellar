# v0.2.27

## Changes

- Add launch promotion pricing to the billing plans.
- Pro Mensal: de R$ 49,90 por R$ 25,45/mês.
- Pro Anual: de R$ 499,00 por R$ 254,50/ano.
- Show the launch promotion label and crossed-out original price on `/billing`.
- Keep paid subscription buttons disabled as `Em breve`.
- Bump app version to `0.2.27` and service worker cache to `minha-adega-v24`.

## Validation

- `pnpm --filter @workspace/minha-adega typecheck`
- `pnpm --filter @workspace/api-server typecheck`
- `pnpm --filter @workspace/minha-adega build`
- `pnpm --filter @workspace/api-server build`
- Production `/api/healthz` returned `{"status":"ok"}`.
- Production `/sw.js` contains `minha-adega-v24`.
- Production bundle contains `0.2.27`, `Promocao de lancamento`, and `Em breve`.
- Production `/api/billing/plans` returned Pro Mensal `amount=25.45`, `originalAmount=49.9`; Pro Anual `amount=254.5`, `originalAmount=499`.

## Rollback

- Roll back app deploy to `v0.2.26` if the pricing display causes unexpected behavior.
