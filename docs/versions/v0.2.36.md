# v0.2.36

## Changes

- Add `wines.additional_photo_url` for the `Fotos adicionais` field.
- Keep `labelPhotoUrl` reserved for the main label photo used by label reading and wine identification.
- Update the wine form so extra photos no longer replace the main wine image.
- Show the additional photo separately on the wine detail page when present.
- Bump app/API/workspace version to `0.2.36` and service worker cache to `minha-adega-v32`.

## Validation

- `pnpm --filter @workspace/api-spec run codegen` passed.
- `pnpm --filter @workspace/minha-adega typecheck` passed.
- `pnpm --filter @workspace/api-server typecheck` passed.
- `pnpm --filter @workspace/api-server build` passed.
- `PORT=3000 BASE_PATH=/ pnpm --filter @workspace/minha-adega build` passed with known non-blocking sourcemap/chunk warnings.

## Rollback

- Roll back app deploy to `v0.2.35` if the additional-photo schema or UI change causes unexpected behavior.
- The database column is additive and can remain in place during rollback.
