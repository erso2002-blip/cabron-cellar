# v0.2.21

## Changes

- Add a clear `Já tenho acesso` entry point on the public waitlist header.
- Add a secondary `Entrar no beta fechado` button under the waitlist submit button.
- Keep `/` as public waitlist traffic and `/login` as the approved tester login route.
- Keep server-side closed beta allowlist enforcement from `v0.2.20`.
- Bump app version to `0.2.21` and service worker cache to `minha-adega-v19`.

## Validation

- Local `pnpm --filter @workspace/minha-adega run typecheck` passed.
- Local `PORT=3000 BASE_PATH=/ pnpm --filter @workspace/minha-adega run build` passed.

## Rollback

- Roll back app deploy to `v0.2.20` if the new waitlist login entry point causes unexpected routing behavior.
