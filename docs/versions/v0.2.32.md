# v0.2.32

## Changes

- Restrict the `Adega` screen to wines with `quantity >= 1`.
- Keep zero-stock wine records available through detail/history flows instead of showing them in the main cellar list.
- Bump workspace, API, front-end version, and service worker cache.

## Validation

- `pnpm --filter @workspace/minha-adega typecheck` passed.
- `PORT=3000 BASE_PATH=/ pnpm --filter @workspace/minha-adega build` passed with known non-blocking sourcemap/chunk warnings.

## Rollback

- Roll back app deploy to `v0.2.31` if the cellar list filtering causes a user-facing issue.
