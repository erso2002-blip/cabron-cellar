# v0.2.51

Release date: 2026-06-22

## Changes

- Fix Google SSO verification to accept both `GOOGLE_CLIENT_ID` and `VITE_GOOGLE_CLIENT_ID` as valid token audiences.
- Ensure account/profile database columns and tables are created before Google or e-mail login upserts a user.
- Add controlled server log reason for Google session creation failures.
- Bump app/API/workspace version to `0.2.51`, native build numbers to `51`, and service worker cache to `minha-adega-v46`.

## Validation

- Front-end TypeScript passed.
- API TypeScript passed.
- API production build passed.
- Front-end production build passed with known non-blocking sourcemap/chunk warnings.
- Capacitor sync for iOS/Android passed.
- Vercel production deployment `dpl_9KizSKDA7XHnoep6pD8PryNBU1Ew` reached `READY` and alias `https://mycellar.com.br` was applied.
- Public validation passed for `/api/healthz`, `/api/auth/config`, service worker cache `minha-adega-v46`, and public bundle `0.2.51`.
- Expanded Vercel logs confirmed the prior login failure was a production database schema mismatch: `users.phone` did not exist before the Google user upsert.

## Rollback

- Roll back app deploy to `v0.2.50` if Google SSO behavior regresses.
