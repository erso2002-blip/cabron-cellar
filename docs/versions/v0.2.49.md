# v0.2.49

Release date: 2026-06-22

## Changes

- Return a clear closed-beta access message when Google login succeeds but the e-mail is not allowed.
- Bump app/API/workspace version to `0.2.49`, native build numbers to `49`, and service worker cache to `minha-adega-v44`.

## Validation

- Front-end TypeScript passed.
- API TypeScript passed.
- API production build passed.
- Front-end production build passed with known non-blocking sourcemap/chunk warnings.
- Capacitor sync for iOS/Android passed.
- Vercel production deployment `dpl_8fZr2G85iGoZ3oA2cV3CL5kVF1Nw` reached `READY` and alias `https://mycellar.com.br` was applied.
- Public validation passed for `/api/healthz`, `/api/auth/config`, service worker cache `minha-adega-v44`, and controlled Google-session bad-token handling.
- Public bundle contains `0.2.49`, `/api/auth/google/session`, and the closed-beta access message.

## Rollback

- Roll back app deploy to `v0.2.48` if the login handling or cache refresh causes unexpected login behavior.
