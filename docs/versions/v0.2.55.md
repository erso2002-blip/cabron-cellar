# v0.2.55

## Changes

- Fix `/api/billing/status` to prioritize active subscriptions before newer pending checkout attempts.
- Bump app/API/workspace version to `0.2.55` and service worker cache to `minha-adega-v50`.

## Validation

- `pnpm --filter @workspace/api-server typecheck`
- `pnpm --filter @workspace/minha-adega typecheck`

## Notes

- Production data confirmed one `authorized` Mercado Pago subscription for `erso2002@gmail.com`, while newer abandoned attempts remained `pending`.
- Mercado Pago webhook events were still absent, so provider-side webhook delivery must still be reviewed after the app-side hotfix.
