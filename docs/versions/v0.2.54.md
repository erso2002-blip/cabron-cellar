# v0.2.54

## Changes

- Add Mercado Pago pending-subscription reconciliation when authenticated users request `/api/billing/status`.
- Update `/billing` to read the authenticated billing status and mark the active plan instead of assuming Free.
- Bump app/API/workspace version to `0.2.54` and service worker cache to `minha-adega-v49`.

## Validation

- `pnpm --filter @workspace/api-server typecheck`
- `pnpm --filter @workspace/minha-adega typecheck`

## Notes

- Webhook delivery still needs provider-side confirmation because no Mercado Pago webhook events were recorded during the first live test.
- The reconciliation fallback avoids leaving an approved subscription stuck as Free when the provider status is already authorized.
