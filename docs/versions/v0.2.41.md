# v0.2.41

## Changes

- Remove `Sem avaliacao` from the `Compraria novamente` history filter, keeping only `Todos`, `Sim`, and `Nao`.
- Standardize consumption and evaluation copy around `Compraria novamente` and sentence-case Portuguese labels.
- Bump app/API/workspace version to `0.2.41` and service worker cache to `minha-adega-v36`.

## Validation

- `pnpm run typecheck` passed.
- `pnpm -r --filter "./artifacts/minha-adega" --if-present run build` passed with known non-blocking sourcemap/chunk warnings.

## Rollback

- Roll back app deploy to `v0.2.40` if the consumption-history filter or copy standardization causes unexpected UI issues.
