# v0.2.40

## Changes

- Add the `Compraria novamente` filter to the consumption history with `Todos`, `Sim`, `Nao`, and `Sem avaliacao` options.
- Update bulk import copy from template/file wording to `planilha modelo` and `enviar arquivo`.
- Bump app/API/workspace version to `0.2.40` and service worker cache to `minha-adega-v35`.

## Validation

- `pnpm --filter @workspace/minha-adega typecheck` passed.
- `PORT=3000 BASE_PATH=/ pnpm --filter @workspace/minha-adega build` passed with known non-blocking sourcemap/chunk warnings.

## Rollback

- Roll back app deploy to `v0.2.39` if the history filter or import copy causes unexpected UI issues.
