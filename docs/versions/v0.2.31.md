# v0.2.31

## Changes

- Add label-photo upload/removal controls to the wine add/edit form.
- Reuse the same label-photo compression/upload utility used by the label scanner.
- Make `Adega` open in compact list mode by default.
- Reduce grid wine cards to smaller label photos with key bottle information beside the image.
- Enrich label analysis with conservative public web search when the photo has limited readable data.
- Position assisted cellar import as a Pro plan feature in billing copy.
- Record cellar import as a Pro-only roadmap feature, with controlled beta exceptions only when approved.
- Bump workspace, API, front-end version, and service worker cache.

## Validation

- `pnpm --filter @workspace/api-server typecheck` passed.
- `pnpm --filter @workspace/minha-adega typecheck` passed.
- `pnpm --filter @workspace/api-server build` passed.
- `PORT=3000 BASE_PATH=/ pnpm --filter @workspace/minha-adega build` passed with known non-blocking sourcemap/chunk warnings.

## Rollback

- Roll back app deploy to `v0.2.30` if the edit-form photo controls, compact cellar layout, label enrichment, or billing copy cause a user-facing issue.
