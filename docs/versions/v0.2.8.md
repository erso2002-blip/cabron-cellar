# v0.2.8

- Public app version: `0.2.8`
- Expands the free-text search in `Sua Adega` to include wine country and region.
- Updates the search placeholder to mention country and region.
- Service worker cache bumped to `minha-adega-v6` to force fresh delivery.

## Validation

- `pnpm --filter @workspace/api-server typecheck` passed.
- `pnpm --filter @workspace/minha-adega typecheck` passed.
- `pnpm --filter @workspace/api-server build` passed.
- `PORT=3000 BASE_PATH=/ pnpm --filter @workspace/minha-adega build` passed.
- `git diff --check` passed.
- Commit `2a5261a` and tag `v0.2.8` were pushed to GitHub.
- Production deployment `cabron-cellar-p2fo2klnt-erso2002-blips-projects.vercel.app` became `Ready`.
- `https://mycellar.com.br/` returned 200.
- `https://www.mycellar.com.br/` returned 200.
- `https://mycellar.com.br/api/healthz` returned `{"status":"ok"}`.
- Public `sw.js` contains `minha-adega-v6`.
- Public bundle contains `0.2.8` and the updated search placeholder.
