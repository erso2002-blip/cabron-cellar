# v0.2.34

## Changes

- Bump app/API/workspace version to `0.2.34`.
- Bump service worker cache from `minha-adega-v29` to `minha-adega-v30` so mobile/PWA clients fetch the latest wine form copy.

## Validation

- `pnpm --filter @workspace/minha-adega typecheck` passed.
- `pnpm --filter @workspace/api-server typecheck` passed.
- `PORT=3000 BASE_PATH=/ pnpm --filter @workspace/minha-adega build` passed with known non-blocking sourcemap/chunk warnings.

## Rollback

- Roll back app deploy to `v0.2.33` if the cache refresh causes unexpected client behavior.
